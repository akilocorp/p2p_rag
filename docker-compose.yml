
services:
  backend:
    build: ./backend
    container_name: rag_platform_backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    networks:
      - rag_network

  frontend:
    build: ./frontend
    container_name: rag_platform_frontend
    ports:
      - "5173:5173"
    networks:
      - rag_network
    depends_on:
      - backend

networks:
  rag_network:
    driver: bridge
